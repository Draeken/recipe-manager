import { TypographyTheme } from '@recipes-manager/ui';

const typoTheme: TypographyTheme = {
  typeface: {
    titleTypeface: "'Roboto', sans-serif",
  },
};

export const theme = {
  ...typoTheme,
};
